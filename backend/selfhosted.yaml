# Self-hosted production-style stack for the beta server (no k3s/Flux).
# All services bind to the host loopback only; nginx (NPM) on the same host is
# the single public entrypoint. Run with:
#   docker compose --env-file backend/beta.secrets.env \
#     -f backend/selfhosted.yaml up -d
services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    init: true
    stop_grace_period: 5s
    command:
      - postgres
      - -c
      - log_statement=none
      - -c
      - log_min_error_statement=fatal
      - -c
      - log_min_messages=warning
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "127.0.0.1:5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 20
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7
    restart: unless-stopped
    init: true
    stop_grace_period: 3s
    command: ["sh", "-c", "exec redis-server --save '' --appendonly no --requirepass \"$REDIS_PASSWORD\""]
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$REDIS_PASSWORD\" ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    volumes:
      - redis-data:/data

  gameplay-node:
    image: ${REGISTRY:-ghcr.io/sourcelocation}/geoduels-gameplay-node:${IMAGE_TAG:-beta}
    restart: unless-stopped
    init: true
    stop_grace_period: 5m
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      GAMEPLAY_NODE_ADDR: :8091
      GAMEPLAY_NODE_ID: game-1
      GAMEPLAY_PUBLIC_ROUTE: game-1
      GAMEPLAY_INTERNAL_URL: http://gameplay-node:8091
      POSTGRES_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      APP_AUTH_SECRET: ${APP_AUTH_SECRET}
      GAMEPLAY_TICKET_SECRET: ${GAMEPLAY_TICKET_SECRET}
      COORDINATOR_INTERNAL_SECRET: ${COORDINATOR_INTERNAL_SECRET}
      APP_ACCESS_TOKEN_TTL: ${APP_ACCESS_TOKEN_TTL:-15m}
      APP_REFRESH_TOKEN_TTL: ${APP_REFRESH_TOKEN_TTL:-720h}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  match-coordinator:
    image: ${REGISTRY:-ghcr.io/sourcelocation}/geoduels-match-coordinator:${IMAGE_TAG:-beta}
    restart: unless-stopped
    init: true
    stop_grace_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      MATCH_COORDINATOR_ADDR: :8090
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      POSTGRES_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      APP_AUTH_SECRET: ${APP_AUTH_SECRET}
      GAMEPLAY_TICKET_SECRET: ${GAMEPLAY_TICKET_SECRET}
      COORDINATOR_INTERNAL_SECRET: ${COORDINATOR_INTERNAL_SECRET}
    ports:
      - "127.0.0.1:8090:8090"
    depends_on:
      redis:
        condition: service_healthy
      gameplay-node:
        condition: service_started
      postgres:
        condition: service_healthy

  realtime-gateway:
    image: ${REGISTRY:-ghcr.io/sourcelocation}/geoduels-realtime-gateway:${IMAGE_TAG:-beta}
    restart: unless-stopped
    init: true
    stop_grace_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      REALTIME_GATEWAY_ADDR: :8092
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
    ports:
      - "127.0.0.1:8092:8092"
    depends_on:
      redis:
        condition: service_healthy
      gameplay-node:
        condition: service_started

  api:
    image: ${REGISTRY:-ghcr.io/sourcelocation}/geoduels-api:${IMAGE_TAG:-beta}
    restart: unless-stopped
    init: true
    stop_grace_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      MATCH_COORDINATOR_URL: http://match-coordinator:8090
      POSTGRES_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      APP_AUTH_SECRET: ${APP_AUTH_SECRET}
      GAMEPLAY_TICKET_SECRET: ${GAMEPLAY_TICKET_SECRET}
      COORDINATOR_INTERNAL_SECRET: ${COORDINATOR_INTERNAL_SECRET}
      APP_ACCESS_TOKEN_TTL: ${APP_ACCESS_TOKEN_TTL:-15m}
      APP_REFRESH_TOKEN_TTL: ${APP_REFRESH_TOKEN_TTL:-720h}
      GUEST_SIGNUP_IP_LIMIT: ${GUEST_SIGNUP_IP_LIMIT:-5}
      GUEST_SIGNUP_IP_WINDOW: ${GUEST_SIGNUP_IP_WINDOW:-10m}
      TRUSTED_PROXY_CIDRS: ${TRUSTED_PROXY_CIDRS}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS}
      ADMIN_BOOTSTRAP_EMAILS: ${ADMIN_BOOTSTRAP_EMAILS}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      GOOGLE_ISSUER: ${GOOGLE_ISSUER:-https://accounts.google.com}
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID}
      DISCORD_CLIENT_SECRET: ${DISCORD_CLIENT_SECRET}
      STRIPE_MODE: ${STRIPE_MODE:-test}
      STRIPE_TEST_PAYMENT_LINK_URL: ${STRIPE_TEST_PAYMENT_LINK_URL:-}
      STRIPE_TEST_WEBHOOK_SECRET: ${STRIPE_TEST_WEBHOOK_SECRET:-}
    ports:
      - "127.0.0.1:8080:8080"
    depends_on:
      - match-coordinator

  web:
    image: ${REGISTRY:-ghcr.io/sourcelocation}/geoduels-web:${IMAGE_TAG:-beta}
    restart: unless-stopped
    init: true
    stop_grace_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      PORT: "3000"
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL}
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      NEXT_PUBLIC_QUEUE_URL: ${NEXT_PUBLIC_QUEUE_URL}
      NEXT_PUBLIC_REALTIME_URL: ${NEXT_PUBLIC_REALTIME_URL}
      NEXT_PUBLIC_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      NEXT_PUBLIC_GOOGLE_ALLOWED_ORIGINS: ${NEXT_PUBLIC_GOOGLE_ALLOWED_ORIGINS}
      NEXT_PUBLIC_DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID}
      NEXT_PUBLIC_APP_VERSION: ${IMAGE_TAG:-beta}
    ports:
      - "127.0.0.1:3000:3000"
    depends_on:
      - api

  moderation-worker:
    image: ${REGISTRY:-ghcr.io/sourcelocation}/geoduels-moderation-worker:${IMAGE_TAG:-beta}
    restart: unless-stopped
    init: true
    stop_grace_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      MODERATION_WORKER_ADDR: :8093
      POSTGRES_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      RISK_ENGINE_URL: ${RISK_ENGINE_URL:-}
      RISK_ENGINE_TOKEN: ${RISK_ENGINE_TOKEN:-}
    ports:
      - "127.0.0.1:8093:8093"
    depends_on:
      postgres:
        condition: service_healthy

  discord-worker:
    image: ${REGISTRY:-ghcr.io/sourcelocation}/geoduels-discord-worker:${IMAGE_TAG:-beta}
    restart: unless-stopped
    init: true
    stop_grace_period: 5s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"
    environment:
      DISCORD_WORKER_ADDR: :8094
      POSTGRES_URL: postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN}
    ports:
      - "127.0.0.1:8094:8094"
    depends_on:
      postgres:
        condition: service_healthy

  # One-shot schema migration against the compose postgres. The database is
  # blank on first run, so v2 baseline (version 2000) and later files apply.
  db-migrate:
    image: migrate/migrate:v4.18.3
    profiles: ["tools"]
    volumes:
      - ./db/migrations:/migrations:ro
    command:
      - -path=/migrations
      - -database
      - postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?sslmode=disable
      - up
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres-data:
  redis-data:
