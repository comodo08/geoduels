apiVersion: v1
kind: Service
metadata:
  name: gameplay-node-headless
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: gameplay-node
  ports:
    - name: http
      port: 8091
      targetPort: http
